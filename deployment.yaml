apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapplication-v1
  namespace: kubernetes-demo
  labels:
    app: nginx-v1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx-v1
  template:
    metadata:
      labels:
        app: nginx-v1
    spec:
      containers:
      - name: nginx
        image: kmannedev/practice-imageversion1:v1.0
        ports:
        - containerPort: 80