apiVersion: v1
kind: Service
metadata:
  name: my-app-service
  namespace: kubernetes-demo
spec:
  type: NodePort
  selector:
    app: nginx-v1
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30000